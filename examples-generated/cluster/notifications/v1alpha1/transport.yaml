apiVersion: notifications.authentik.crossplane.io/v1alpha1
kind: Transport
metadata:
  annotations:
    meta.upbound.io/example-id: notifications/v1alpha1/transport
  labels:
    testing.upbound.io/example-name: transport
  name: transport
spec:
  forProvider:
    mode: webhook
    name: my-transport
    sendOnce: true
    webhookUrl: https://foo.bar
